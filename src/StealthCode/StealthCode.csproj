<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <ApplicationIcon>Assets\logo.ico</ApplicationIcon>
    <AvaloniaUseCompiledBindingsByDefault>true</AvaloniaUseCompiledBindingsByDefault>
    <AssemblyName>stealthcode</AssemblyName>
    <Version>1.3.0</Version>
    <Authors>Sukhrob Ilyosbekov</Authors>
    <Product>Stealth Code</Product>
    <AssemblyTitle>Stealth Code</AssemblyTitle>
    <FileDescription>Stealth Code</FileDescription>
  </PropertyGroup>

  <!-- AOT Configuration -->
  <PropertyGroup>
    <PublishAot>true</PublishAot>
    <IsAotCompatible>true</IsAotCompatible>
    <OptimizationPreference>Size</OptimizationPreference>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <InvariantGlobalization>true</InvariantGlobalization>
    <BuiltInComInteropSupport>true</BuiltInComInteropSupport>
    <!-- Without this, published builds report no AVX support and Whisper refuses to run on the CPU. -->
    <IlcInstructionSet>avx</IlcInstructionSet>
  </PropertyGroup>

  <ItemGroup>
    <AvaloniaResource Include="Assets\**"/>
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" Version="12.1.1"/>
    <PackageReference Include="Avalonia.Controls.WebView" Version="12.1.0"/>
    <PackageReference Include="Avalonia.Desktop" Version="12.1.1"/>
    <PackageReference Include="Avalonia.Themes.Fluent" Version="12.1.1"/>
    <PackageReference Include="Avalonia.Fonts.Inter" Version="12.1.1"/>
    <PackageReference Include="AvaloniaUI.DiagnosticsSupport" Version="2.2.3">
      <IncludeAssets Condition="'$(Configuration)' != 'Debug'">None</IncludeAssets>
      <PrivateAssets Condition="'$(Configuration)' != 'Debug'">All</PrivateAssets>
    </PackageReference>
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.2"/>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.11"/>
    <!-- Also excluded in StealthCode.Terminal, but it comes back when this project is published. -->
    <PackageReference Include="Quick.PtyNet.WinPty" Version="1.0.2" ExcludeAssets="all"/>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\StealthCode.Terminal\StealthCode.Terminal.csproj"/>
    <ProjectReference Include="..\StealthCode.ScreenCapture\StealthCode.ScreenCapture.csproj"/>
    <ProjectReference Include="..\StealthCode.Audio\StealthCode.Audio.csproj"/>
    <ProjectReference Include="..\StealthCode.Updater\StealthCode.Updater.csproj"/>
  </ItemGroup>

  <ItemGroup>
    <Compile Update="Views\MainWindow.axaml.cs">
      <DependentUpon>MainWindow.axaml</DependentUpon>
      <SubType>Code</SubType>
    </Compile>
  </ItemGroup>
</Project>
