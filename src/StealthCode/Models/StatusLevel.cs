namespace StealthCode.Models;

/// <summary>Severity travels with the status text so one message renders in one colour.</summary>
public enum StatusLevel
{
    Info,
    Success,
    Warning,
    Error
}
